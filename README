This is a tiny ip address logging/monitoring application

Dependencies:
$ sudo pip install tweepy

What's with the name?
Not much really. Sounds a bit like I(py) P MONitor, but it's totally not like ipmon.
